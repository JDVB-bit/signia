# 🔘 `comunes/` — Componentes base

| Archivo | Qué es |
|---|---|
| `Button.jsx` | Botón del sitio con variantes `primary` / `secondary`, tamaños `md` / `lg` y estado deshabilitado |

```jsx
<Button onClick={guardar}>Guardar</Button>
<Button variant="secondary" size="lg" disabled>Traducir</Button>
```
